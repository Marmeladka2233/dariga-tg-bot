{
  "name": "dariga_tg_bot",
  "version": "1.0.0",
  "description": "Dariga vocal studio booking bot",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix mini-app run dev -- --host\"",
    "build": "npm --prefix backend run build && npm --prefix mini-app run build",
    "test": "echo \"No tests configured yet\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
